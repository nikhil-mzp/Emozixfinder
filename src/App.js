import React from "react";
import { Navbar } from "./components";
import { Result } from "./container";

function App() {
  return (
    <div>
      <Navbar />
      <Result />
    </div>
  );
}

export default App;

//make components and required containers
//1)make header, input , card as result
//2) make two containers header, information
//explore the emoji api
//input the emoji and try to console the data of that emoji
//1) when the find button gets clicked the api call and data get retrieved...
//then code to put the data on the frontend on required component
